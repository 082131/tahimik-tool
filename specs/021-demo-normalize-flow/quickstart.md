# Quickstart: Verify Demo Normalize

Default presentation build:

```powershell
Set-Location frontend
npm run build
```

For live behavior, set `VITE_TAHIMIK_DATA_MODE=live` before starting/building the frontend and ensure the backend is available at `VITE_API_URL` or `http://localhost:8100`.
