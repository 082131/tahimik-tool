# Tasks: ByT5 Baseline (No Compression)

**Input**: Design documents from `/specs/003-byt5-baseline/`

**Prerequisites**: spec.md, plan.md, research.md, data-model.md, quickstart.md
(no contracts/ — internal component)

**Tests**: Mandatory, not optional. `src/models/byt5_baseline.py` is under
`src/models/`, so Constitution Principle IV requires test-first.

**Retrofit note**: The component already exists and is already correct. Phase 3
tasks are therefore written as *verify*, not *build* — Phase 6 records what
`/speckit-converge` actually found.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Different file, no dependency on an incomplete task.
- **[Story]**: Maps to spec.md's User Story 1/2.

## Phase 1: Setup

None. No new dependency, directory, or config surface.

## Phase 2: Foundational

None. Everything this feature needs already exists, including the tiny-T5 test
pattern in `tests/test_model_forward.py`.

**Checkpoint**: User Story 1 can start immediately.

---

## Phase 3: User Story 1 - Establish the accuracy ceiling (Priority: P1) 🎯 MVP

**Goal**: Prove the baseline trains, deletes nothing, and generates.

**Independent Test**: `pytest tests/test_byt5_baseline.py -v` against a tiny
randomly-initialized T5.

### Tests for User Story 1 ⚠️

> Write first, confirm failing, then implement.

- [ ] T001 [US1] Write shape-and-loss test in `tests/test_byt5_baseline.py`: forward pass with labels returns a finite loss and logits shaped `(batch, target_len, vocab)` (SC-001)
- [ ] T002 [US1] Write zero-deletion test in `tests/test_byt5_baseline.py`: assert `deletion_rate` is exactly `0.0` for every sentence, including a batch with padding (SC-002)
- [ ] T003 [US1] Write generation test in `tests/test_byt5_baseline.py`: `generate()` returns byte IDs with batch dimension preserved (SC-003)
- [ ] T004 [US1] Write a structural test asserting `byt5_baseline.py` imports neither `DeleteGate` nor `NoiseEstimator` — the absence is the requirement (FR-001)
- [ ] T005 [US1] Run `python -m pytest tests/test_byt5_baseline.py -v` and confirm the tests fail before any implementation change

### Implementation for User Story 1

- [ ] T006 [P] [US1] Verify `src/models/byt5_baseline.py` wraps `T5ForConditionalGeneration` with no gate and no estimator (FR-001)
- [ ] T007 [P] [US1] Verify the model and tokenizer both load from `config.model_name` (FR-002)
- [ ] T008 [US1] Verify `deletion_rate` is emitted as an exact zero tensor sized to the batch (FR-003)
- [ ] T009 [US1] Verify the forward-pass result carries `loss`, `logits`, `encoder_last_hidden_state`, and `deletion_rate` (FR-004)
- [ ] T010 [US1] Run the suite again and confirm T001–T004 pass

**Checkpoint**: The accuracy ceiling is verifiable without a dataset.

---

## Phase 4: User Story 2 - Serve as the efficiency floor (Priority: P2)

**Goal**: The baseline produces the timing and memory numbers RQ2 measures
everything else against.

**Status**: Partially blocked. The benchmark harness belongs to spec `007`, and
real numbers need a GPU plus the gold-standard test set.

### Tests for User Story 2

- [ ] T011 [US2] Verify `generate()` is callable through the same interface `src/evaluation/efficiency.py` uses, so the benchmark needs no variant-specific branch (FR-005)

### Implementation for User Story 2

None. The benchmark harness is spec `007`'s responsibility. **BLOCKED, not
skipped**: SC-004 and SC-005 need real data and a GPU.

**Checkpoint**: Interface confirmed benchmark-ready.

---

## Final Phase: Polish

- [ ] T012 [P] Run `quickstart.md` end to end, including both hand-check commands, and correct the doc if reality diverges
- [ ] T013 Record in the PR description which Phase 3 tasks converge found already satisfied versus newly written

---

## Phase 6: Convergence

**Generated by `/speckit-converge`** after reading
`src/models/byt5_baseline.py`, `configs/base.py`, and `configs/byt5_config.py`.

**Headline result**: FR-001, FR-002, FR-003, and FR-005 are already correctly
implemented. The class is a clean minimal wrapper; `deletion_rate` is explicitly
zeroed at `byt5_baseline.py:68`. What is missing is the test file, plus two
recorded divergences.

- [ ] T014 **HIGH** Flag the `byt5-small` / `byt5-base` divergence in a comment at `configs/base.py:25` naming the manuscript as authoritative, per Constitution Principle III (contradicts) — cross-cutting with 004 and 005, fix once
- [ ] T015 [P] Decide whether the three variants should share a uniform output-key contract, or whether the evaluator should branch per variant. `research.md` records the argument on both sides and does not resolve it (partial) — **author decision**
- [ ] T016 [P] Write `tests/test_byt5_baseline.py`, which does not currently exist. No test anywhere covers this variant in isolation (missing)

**Checkpoint**: After T014–T016, run `/speckit-analyze` to confirm spec, plan,
and tasks still agree.

---

## Dependencies & Execution Order

- **Setup / Foundational**: empty — Story 1 starts immediately.
- **User Story 1 (P1)**: no dependency on other stories. MVP.
- **User Story 2 (P2)**: depends on Story 1; otherwise blocked on spec `007`
  and on hardware.
- **T014** is shared with specs `004` and `005`. Do it once, in one place.

### Within User Story 1

T001–T005 (tests) precede T006–T010 (verification). T006 and T007 are marked
`[P]` because they inspect different concerns and neither edits a file; T008
and T009 follow because they read the same forward-pass result.

## Parallel Example

```bash
# T014 and T016 touch different files with no dependency:
Task: "Flag the model-size divergence in configs/base.py"
Task: "Write tests/test_byt5_baseline.py"
```

## Implementation Strategy

**MVP**: Phase 3 alone. It proves the baseline is genuinely uncompressed and
trains correctly, using only synthetic input. That is the whole of what can be
verified without the dataset.

**Then**: T014 (shared divergence flag), T016 (the missing test file), and T015
whenever you have a view on output-key uniformity.

## Notes

- Every Phase 3 implementation task is "verify", not "build" — converge
  confirmed the code already satisfies them.
- Commit at checkpoints, not per task, per `docs/agents/contributing.md`.
