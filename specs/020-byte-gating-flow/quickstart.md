# Quickstart: Verify Byte-Gating Flow

```powershell
Set-Location frontend
npm run build
```

Open the Engine and verify the three stages, fixed/adaptive descriptions, exact byte counts, and unavailable state without byte positions.
